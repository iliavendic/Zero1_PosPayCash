<div id="pos-pay-cash">
    <label for="custom_amount">Amount tendered: </label>
    <input type="text" id="custom_amount" name="custom_amount" wire:model.defer="amountTendered" wire:keydown.enter="save">
    <p>Press enter when complete</p>

    <br>
    <br>

    <?php if($magewire->getChange() > 0): ?>
        <p>Change: <?= $magewire->getChange() ?></p>
    <?php endif; ?>
</div>